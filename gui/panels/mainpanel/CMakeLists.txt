file(GLOB SOURCES *.cpp)
list (REMOVE_ITEM SOURCES moc_*.cpp)
list (REMOVE_ITEM SOURCES *_automoc.cpp)
add_library(mainpanel STATIC ${SOURCES})
target_link_libraries(mainpanel Qt5::Widgets customwidgets)
