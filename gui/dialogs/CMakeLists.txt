file(GLOB SOURCES *.cpp)
list (REMOVE_ITEM SOURCES moc_*.cpp)
list (REMOVE_ITEM SOURCES *_automoc.cpp)
add_library(dialogs STATIC ${SOURCES})
target_link_libraries(dialogs Qt5::Widgets customwidgets)
