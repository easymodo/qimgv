file(GLOB SOURCES *.cpp)
list (REMOVE_ITEM SOURCES moc_*.cpp)
list (REMOVE_ITEM SOURCES *_automoc.cpp)
add_library(directorymanager STATIC ${SOURCES})
target_link_libraries(directorymanager Qt5::Widgets)
