file(GLOB SOURCES *.cpp)
list (REMOVE_ITEM SOURCES moc_*.cpp)
list (REMOVE_ITEM SOURCES *_automoc.cpp)
add_library(loader STATIC ${SOURCES})
target_link_libraries(loader Qt5::Widgets Qt5::Concurrent cache)
